description: |
  Card Sets
content:
  application/json:
    schema:
      type: array
      items:
        type: object
        title: CardSetItemDTO
        properties:
          set_name:
            type: string
          set_code:
            type: string
          num_of_cards:
            type: integer
          tcg_date:
            type: string
            format: date
