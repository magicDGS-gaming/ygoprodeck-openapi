description: |
  Card Archetypes

  All of the current Yu-Gi-Oh! Card Archetype Names stored in the database (sorted by A-Z).
content:
  application/json:
    schema:
      type: array
      title: ArchetypesDTO
      items:
        type: object
        title: ArchetypesItemDTO
        properties:
          archetype_name:
            type: string
