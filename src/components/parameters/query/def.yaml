name: def
in: query
description: |
  Filter by def value.
  
  You can also use the following equation symbols\: 
   - `lt` - less than
   - `lte` - less than equals to
   - `gt` - greater than
   - `gte` - greater than equals to
required: false
schema:
  type: string
  # TODO: add pattern to identify number or the keywords!